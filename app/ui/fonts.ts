import { Inter, Lusitana, Sura } from "next/font/google";

export const inter = Sura({ subsets: ["latin"], weight: "400" });

export const lusitana = Sura({
  subsets: ["latin"],
  weight: "400",
});
